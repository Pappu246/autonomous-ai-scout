# Optional provider credentials
GEMINI_API_KEY=
GROQ_API_KEY=
HF_TOKEN=

# Optional free benchmark controls
# Benchmarking remains opt-in; this only caps free-tier calls per run.
ENABLE_FREE_BENCHMARKS=false
MAX_FREE_BENCHMARKS=2

# Optional Gmail SMTP reporting
SMTP_USERNAME=
SMTP_APP_PASSWORD=
REPORT_EMAIL=
