# Google Gemini Developer API key (optional for future LLM-powered planning)
GEMINI_API_KEY=

# Hugging Face token (optional; disabled by default until free access is verified)
HF_TOKEN=

# Gmail/API settings will be added only after the reporting transport is configured.
