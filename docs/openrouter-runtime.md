# OpenRouter runtime

Optional runtime planning uses only `openrouter/free`, requires `OPENROUTER_API_KEY`, and has no paid fallback. Responses are bounded to eight planning steps. This adapter remains separate from automatic task execution until explicitly integrated and tested.
